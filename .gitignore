# deps
node_modules/
.pnp.*

# builds
dist/
build/
.output/
*.tsbuildinfo

# cloudflare / wrangler
.wrangler/
.wrangler-shared/
.dev.vars
.dev.vars.*
!.dev.vars.example
worker-configuration.d.ts

# drizzle local db artifacts
*.sqlite
*.sqlite-*

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
bun-debug.log*

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# omc state (not part of the product)
.omc/

# internal working docs (kept locally, not published)
CLAUDE.md
*PLAN.md
*PROGRESS.md
*PROGRESS.html

# local CLI build (releases build from source)
packages/cli/postplan

# generated by `bun run cf-typegen` (wrangler types) — needed for typecheck, not source
packages/api/worker-configuration.d.ts

# Real instance config — this repo is public, so only the *.example siblings are tracked.
# scripts/apply-config.sh regenerates all of these from deploy.env.
deploy.env
/wrangler.jsonc
/wrangler.content.jsonc
packages/web/public/_headers
