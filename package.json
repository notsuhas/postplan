{
  "name": "postplan",
  "private": true,
  "type": "module",
  "description": "An open-source alternative to Claude Artifacts for every coding agent — self-hosted on Cloudflare.",
  "author": "Plivo Inc.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/notsuhas/postplan.git"
  },
  "homepage": "https://github.com/notsuhas/postplan#readme",
  "bugs": {
    "url": "https://github.com/notsuhas/postplan/issues"
  },
  "keywords": [
    "static-hosting",
    "cloudflare-workers",
    "hono",
    "r2",
    "d1"
  ],
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/api",
    "packages/web"
  ],
  "overrides": {
    "esbuild": "0.28.2"
  },
  "scripts": {
    "dev": "trap 'kill 0' SIGINT; bun run dev:api & bun run dev:content & bun run dev:web & wait",
    "dev:api": "bunx wrangler dev -c wrangler.jsonc --local --port 8787 --persist-to .wrangler-shared",
    "dev:content": "bunx wrangler dev -c wrangler.content.jsonc --port 8788 --inspector-port 9230 --persist-to .wrangler-shared",
    "dev:web": "cd packages/web && bunx vite",
    "build:web": "cd packages/web && bunx vite build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test scripts && bun run --filter '*' test",
    "lint": "biome lint .",
    "format": "biome check --write .",
    "format:check": "biome check .",
    "dead-code": "fallow dead-code --type-aware",
    "cf-typegen": "bunx wrangler types -c wrangler.example.jsonc packages/api/worker-configuration.d.ts",
    "db:generate": "cd packages/api && bunx drizzle-kit generate",
    "db:migrate:local": "bunx wrangler d1 migrations apply postplan-db -c wrangler.jsonc --local --persist-to .wrangler-shared",
    "db:migrate:remote": "bunx wrangler d1 migrations apply postplan-db -c wrangler.jsonc --remote",
    "deploy": "scripts/apply-config.sh && bun run build:web && bunx wrangler deploy -c wrangler.jsonc && bunx wrangler deploy -c wrangler.content.jsonc"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.1",
    "fallow": "3.22.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
